#include <iostream>

using namespace std;

int main()
{
    while (true)
    {
        int n, m;
        cin >> n >> m;

        if (n == 0) break;

        int numbers[n];

        for (int i = 0; i < n)

        for (int i = 0; i < n; i++)
        {
            int number;
            cin >> number;
        }
    }
    return 0;
}
