#include <iostream>

using namespace std;

int n, m, k, l;

int main()
{
    int nInputs;
    cin >> nInputs;
    for (int nCase = 1; nCase < nInputs + 1; nCase++) {
        cin >> n >> m;
        for (int i = 0; i < n; i++) {
            cin >> l;
            for (int j = 0; j < k; j++) {
                cin >> l;


            }
        }
        cout << "Case#" << nCase << ": " << 1 << endl;
    }
    return 0;
}
